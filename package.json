{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
		"db:stop": "docker-compose stop db",
    "db:start": "docker-compose start db",
		"db:remove": "docker-compose down",
		"db:create": "docker-compose up -d",
    "db:setup": "docker-compose down && docker-compose up -d && node dist/db/setup.js",
    "build": "tsc --build tsconfig.json ",
    "build:watch": "tsc --watch",
    "start": "npm run db:start && node dist/api/index.js",
    "shutdown": "npm run db:stop"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.6",
    "express": "^4.17.1",
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
