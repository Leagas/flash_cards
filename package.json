{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "db:stop": "docker-compose stop db",
    "db:start": "docker-compose start db",
    "db:remove": "docker-compose down",
    "db:create": "docker-compose up -d",
		"db:setup": "node dist/db/setup.js",
		"setup": "npm run db:remove && npm run db:create && npm run db:setup",
    "build": "tsc --build tsconfig.json ",
    "build:watch": "tsc --build tsconfig.json --watch",
    "start": "npm run db:start && node dist/api/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.6",
    "@types/mysql": "^2.15.14",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "prettier": "^2.0.5",
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
